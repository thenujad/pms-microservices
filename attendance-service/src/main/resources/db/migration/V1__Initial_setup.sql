-- Create the database
CREATE DATABASE IF NOT EXISTS pms_db;
USE pms_db;

-- To trigger Flyway migrations manually:
-- bash Command,
              -- mvn flyway:migrate --
              -- ./gradlew flywayMigrate --

