-- Create the database
CREATE DATABASE IF NOT EXISTS pms_employees_db;
USE pms_employees_db;

-- To trigger Flyway migrations manually:
-- bash Command,
              -- mvn flyway:migrate --
              -- ./gradlew flywayMigrate --

