-- Create the database
CREATE DATABASE IF NOT EXISTS pms_project_db;
USE pms_project_db;

-- To trigger Flyway migrations manually:
-- bash Command,
              -- mvn flyway:migrate --
              -- ./gradlew flywayMigrate --

