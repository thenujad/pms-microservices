-- Create the database
CREATE DATABASE IF NOT EXISTS pms_users_db;
USE pms_users_db;

-- To trigger Flyway migrations manually:
-- bash Command,
              -- mvn flyway:migrate --
              -- ./gradlew flywayMigrate --

